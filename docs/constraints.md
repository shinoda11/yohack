# YOHACK 制約定義
> このファイルはバックログ実行前に必ず読むこと。

## 使わない技術（Phase1の間は絶対に触らない）
- Supabase（認証・DB）→ 別途判断するまで禁止
- Stripe → 別途判断するまで禁止
- 新規npmパッケージの導入 → 事前承認が必要

## バックログ実行ルール
- 変更してよいファイルはタスクに明示されたもののみ
- 環境変数が必要な実装はしない
- 既存のテストが252件passしていることを必ず確認してからコミット
